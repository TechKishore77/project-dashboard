import { CommercialUpdateRequest } from './commercial-update-request';

describe('CommercialUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new CommercialUpdateRequest()).toBeTruthy();
  });
});
