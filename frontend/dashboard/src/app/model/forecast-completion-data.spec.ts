import { ForecastCompletionData } from './forecast-completion-data';

describe('ForecastCompletionData', () => {
  it('should create an instance', () => {
    expect(new ForecastCompletionData()).toBeTruthy();
  });
});
