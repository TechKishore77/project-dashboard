import { ProjectProgressData } from './project-progress-data';

describe('ProjectProgressData', () => {
  it('should create an instance', () => {
    expect(new ProjectProgressData()).toBeTruthy();
  });
});
